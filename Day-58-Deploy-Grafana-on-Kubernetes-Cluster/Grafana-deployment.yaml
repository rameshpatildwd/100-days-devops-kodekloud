apiVersion: apps/v1
kind: Deployment
metadata:
    name: grafana-deployment-datacenter
spec:
    replicas: 3
    selector:
        matchLabels:
            app: grafana
    template:
        metadata:
            name: grafana-pod
            labels:
                app: grafana
        spec:
            containers:
                - name: grafana-container
                  image: grafana/grafana-enterprise
                  ports:
                      - containerPort: 3000